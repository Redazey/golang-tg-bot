version: '3.8'

services:
    tgssnPg:
        image: postgres:latest
        container_name: tghentPG
        environment:
            - POSTGRES_USER=backend!
            - POSTGRES_PASSWORD=backend1q!Q
            - POSTGRES_DB=tghentPG
        ports:
            - "5432:5432"

    tgssnRedis:
        image: redis
        container_name: tghentRedis
        environment:
            - REDIS_PASSWORD=backend1q!Q
        ports:
            - "6379:6379"

    bot:
        container_name: tgprivatebot
        build:
            target: dev
            network: host
        volumes:
            - .:/app